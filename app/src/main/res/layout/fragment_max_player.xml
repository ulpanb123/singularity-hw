<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.motion.widget.MotionLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/max_player_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/white"
    app:layoutDescription="@xml/max_player_scene">

    <ImageView
        android:scaleType="centerCrop"
        android:id="@+id/img_movie"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:src="@drawable/film1"
        app:layout_constraintTop_toTopOf="parent"
        />

    <TextView
        android:id="@+id/tv_movieName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="8dp"
        android:text="@string/movie1_name"
        android:textAppearance="@style/TabTextStyle"
        app:layout_constraintBottom_toTopOf="@id/tv_movieDescrp"
        app:layout_constraintTop_toBottomOf="@id/img_movie"
        tools:layout_editor_absoluteX="16dp" />

    <TextView
        android:id="@+id/tv_movieDescrp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/movie1_descrp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        app:layout_constraintTop_toBottomOf="@id/tv_movieName"/>

</androidx.constraintlayout.motion.widget.MotionLayout>